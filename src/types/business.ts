import type { BusinessSummary, PlanSummary, SystemMetrics, SystemEventsSnapshot } from '../services/api';

export type Business = BusinessSummary;
export type Plan = PlanSummary;
export type Metrics = SystemMetrics;
export type EventSnapshot = SystemEventsSnapshot;
